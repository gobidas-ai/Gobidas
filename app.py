import streamlit as st
from groq import Groq
import json, os, base64, io, time
from PIL import Image
import streamlit.components.v1 as components

# --- 1. CONFIG & LOCK-OPEN SIDEBAR ---
st.set_page_config(page_title="Gobidas Beta", layout="wide", initial_sidebar_state="expanded")

def get_base64(file):
    if os.path.exists(file):
        with open(file, 'rb') as f:
            return base64.b64encode(f.read()).decode()
    return None

bin_str = get_base64('background.jpg')

st.markdown(f"""
<style>
    /* HIDE TOP HEADER AND COLLAPSE ARROW */
    [data-testid="stHeader"] {{ display: none !important; }}
    [data-testid="stSidebarCollapseButton"] {{ display: none !important; }}
    .stDeployButton, footer, [data-testid="stStatusWidget"] {{ display: none !important; }}
    
    /* FIX SIDEBAR WIDTH */
    [data-testid="stSidebar"] {{
        min-width: 320px !important;
        max-width: 320px !important;
    }}

    /* APP DESIGN */
    .stApp {{
        background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), 
                    url("data:image/jpeg;base64,{bin_str if bin_str else ''}");
        background-size: cover;
    }}
    .main-title {{
        font-weight: 900; color: #FF6D00; text-align: center; font-size: 4.5rem;
        text-shadow: 0px 0px 20px rgba(255, 109, 0, 0.5);
    }}
    [data-testid="stSidebar"] {{
        background-color: rgba(10, 10, 10, 0.98) !important;
        border-right: 2px solid #FF6D00;
    }}
    .stButton>button {{
        width: 100%; border-radius: 10px; border: 1px solid #FF6D00 !important;
        color: white !important; background: transparent; font-weight: bold;
    }}
    .stButton>button:hover {{
        background: #FF6D00 !important; color: black !important;
    }}
</style>
""", unsafe_allow_html=True)

# --- 2. DATABASE & LONG-FORM LEGAL ---
DB_FILE = "gobidas_db.json"
def load_db():
    if os.path.exists(DB_FILE):
        try:
            with open(DB_FILE, "r") as f:
                data = json.load(f)
                now = time.time()
                for u in data.get("history", {}):
                    data["history"][u] = [c for c in data["history"][u] if (now - c.get("ts", now)) < 2592000]
                return data
        except: pass
    return {"users": {}, "history": {}}

def save_db(data):
    with open(DB_FILE, "w") as f: json.dump(data, f)

if "db" not in st.session_state:
    st.session_state.db = load_db()

def show_legal():
    st.markdown("## Comprehensive Terms of Service & Privacy Agreement")
    st.error("### **BETA VERSION NOTICE**\nGobidas Artificial Intelligence (the 'Software') is currently in a Beta testing phase. Users acknowledge that the Software is provided 'as-is' and may contain bugs, errors, or generate inaccurate, biased, or inappropriate content.")
    
    st.markdown("### 1. Limitation of Liability & Indemnification")
    st.write("""
    **A. Third-Party Models:** Gobidas acts solely as a user interface for Large Language Models (LLMs) provided by third parties such as Meta (Llama) and hosted via Groq Cloud. The developer of Gobidas does not control, edit, or filter the logic or outputs generated by these models.
    
    **B. No Warranty:** The developer expressly disclaims any liability for damages arising from the use of this Software. This includes, but is not limited to, legal disputes, financial loss, or personal distress caused by AI-generated responses.
    
    **C. Indemnity:** By proceeding, the user agrees to indemnify and hold harmless the developer from any claims, losses, or legal fees resulting from the user's reliance on AI outputs or any violation of these terms.
    """)

    st.markdown("### 2. User Responsibilities")
    st.write("""
    Users are strictly prohibited from using Gobidas for:
    * Generating illegal, harmful, or threatening content.
    * Attempting to bypass safety filters built into the underlying LLMs.
    * Engaging in activities that violate the privacy or rights of others.
    * Using AI-generated advice for medical, financial, or legal decisions without professional consultation.
    """)

    st.markdown("### 3. Privacy & Data Retention")
    st.write("""
    * **Local Storage Architecture:** To maximize user privacy, your credentials and chat history are stored in a local JSON database on the host machine. No data is stored on external 'Gobidas' servers.
    * **The 30-Day Purge:** To ensure data hygiene and security, the system executes an automatic deletion script. Any chat entry or interaction older than 30 days (2,592,000 seconds) is permanently erased and cannot be recovered.
    * **External Processing:** Text and images submitted are transmitted to Groq Cloud for inference. Users should consult Groq's privacy policy regarding data handling during processing.
    """)

# --- 3. LOGIN SCREEN ---
if "user" not in st.session_state:
    st.markdown("<h1 class='main-title'>Gobidas</h1>", unsafe_allow_html=True)
    c1, c2, c3 = st.columns([1, 1.8, 1])
    with c2:
        mode = st.radio(" ", ["Log In", "Sign Up"], horizontal=True)
        u = st.text_input("Name")
        p = st.text_input("Password", type="password")
        st.caption("Accessing Gobidas requires agreement to the long-form terms below.")
        agree = st.checkbox("I have read and agree to the Terms of Service and Privacy Policy")
        
        if st.button("Enter Gobidas", disabled=not agree):
            if mode == "Log In":
                if u in st.session_state.db["users"] and st.session_state.db["users"][u] == p:
                    st.session_state.user = u
                    st.session_state.messages = []
                    st.rerun()
                else: st.error("Access Denied: Invalid Credentials.")
            else:
                if u and p:
                    st.session_state.db["users"][u] = p
                    st.session_state.db["history"][u] = []
                    save_db(st.session_state.db)
                    st.success("Registration Successful. Please log in.")
        
        st.divider()
        with st.expander("VIEW FULL TERMS AND CONDITIONS"):
            show_legal()
    st.stop()

# --- 4. SIDEBAR (LOCKED OPEN) ---
with st.sidebar:
    st.title(f"User: {st.session_state.user}")
    
    if st.button("➕ New Chat Session"):
        st.session_state.messages = []
        st.session_state.active_idx = None
        st.rerun()
    
    img_file = st.file_uploader("Vision: Upload Image", type=['png', 'jpg', 'jpeg'])
    
    st.divider()
    st.write("### Active History")
    
    hist_list = st.session_state.db["history"].get(st.session_state.user, [])
    
    for i in range(len(hist_list)-1, -1, -1):
        chat = hist_list[i]
        c_chat, c_del = st.columns([0.8, 0.2])
        
        with c_chat:
            if st.button(chat.get('name', 'Chat'), key=f"open_{i}"):
                st.session_state.messages = chat.get("msgs", [])
                st.session_state.active_idx = i
                st.rerun()
        with c_del:
            if st.button("🗑️", key=f"del_{i}"):
                hist_list.pop(i)
                st.session_state.db["history"][st.session_state.user] = hist_list
                save_db(st.session_state.db)
                if st.session_state.get("active_idx") == i:
                    st.session_state.messages = []
                    st.session_state.active_idx = None
                st.rerun()

    st.divider()
    if st.button("⚙️ System Settings"):
        st.session_state.show_settings = not st.session_state.get("show_settings", False)
    
    if st.session_state.get("show_settings"):
        if st.button("Logout"):
            del st.session_state.user
            st.rerun()
        with st.expander("Legal & Privacy Docs"):
            show_legal()

# --- 5. CHAT ENGINE ---
client = Groq(api_key=st.secrets["GROQ_API_KEY"])
st.markdown("<h1 class='main-title'>Gobidas</h1>", unsafe_allow_html=True)

for msg in st.session_state.messages:
    with st.chat_message(msg["role"]): st.markdown(msg["content"])

if prompt := st.chat_input("Input command..."):
    st.session_state.messages.append({"role": "user", "content": prompt})
    with st.chat_message("user"):
        st.markdown(prompt)
        if img_file: st.image(img_file, width=300)

    with st.chat_message("assistant"):
        try:
            if img_file:
                img = Image.open(img_file).convert("RGB")
                img.thumbnail((800, 800))
                buf = io.BytesIO()
                img.save(buf, format="JPEG")
                b64_str = base64.b64encode(buf.getvalue()).decode('utf-8')
                
                res = client.chat.completions.create(
                    model="meta-llama/llama-4-scout-17b-16e-instruct",
                    messages=[{"role": "user", "content": [
                        {"type": "text", "text": prompt},
                        {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{b64_str}"}}
                    ]}]
                )
            else:
                res = client.chat.completions.create(
                    model="llama-3.3-70b-versatile", 
                    messages=st.session_state.messages
                )
            
            ans = res.choices[0].message.content
            st.markdown(ans)
            st.session_state.messages.append({"role": "assistant", "content": ans})
            
            # Auto-save history
            history = st.session_state.db["history"].get(st.session_state.user, [])
            chat_title = prompt[:22] + "..." if len(prompt) > 22 else prompt
            chat_entry = {"name": chat_title, "msgs": st.session_state.messages, "ts": time.time()}
            
            if st.session_state.get("active_idx") is None:
                history.append(chat_entry)
                st.session_state.active_idx = len(history) - 1
            else:
                history[st.session_state.active_idx] = chat_entry
            
            st.session_state.db["history"][st.session_state.user] = history
            save_db(st.session_state.db)
            
        except Exception as e:
            st.error(f"System Error: {e}")
